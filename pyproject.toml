[project]
name = "agentic-artifact-loop"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.0.0",
  "openai>=1.30.0",
  "python-dotenv>=1.0.0"
]

[tool.setuptools.packages.find]
where = ["src"]
